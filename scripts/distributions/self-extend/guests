# The coding distribution's fleet, plus a shell — because compiling is running
# a command, and nothing else here can. Everything in `coding/guests` is here
# for the same reasons it is there; the last line is the whole difference.
provider-openai
provider-anthropic
interceptor-intent-router
interceptor-task-router
interceptor-context
interceptor-system
interceptor-tool-selector
interceptor-permission
interceptor-guardrails
registry-skills
tool-fs
tool-edit
tool-find
tool-git
tool-shell

[package]
name = "tool-hello"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "A tool extension. Replace this description with what it does."

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen.workspace = true

[lints]
workspace = true

[package]
name = "interceptor-guardrails"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "content-policy interceptor: matches configured patterns against tool arguments and model output, and blocks, asks, or redacts on the rule's decision."

[lib]
# A reactor component: exports interceptor-world, no `main`.
crate-type = ["cdylib"]

[dependencies]
wit-bindgen.workspace = true
serde_json.workspace = true
regex.workspace = true

[lints]
workspace = true

[package]
name = "tool-edit"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "edit tool: hash-anchored line edits of a workspace file through host-fs; stale anchors are rejected, never applied."

[lib]
# A reactor component: exports tool-world, no `main`.
crate-type = ["cdylib"]

[dependencies]
wit-bindgen.workspace = true
serde_json.workspace = true

[lints]
workspace = true

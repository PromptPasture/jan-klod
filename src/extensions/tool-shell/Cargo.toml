[package]
name = "tool-shell"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "shell tool: run a command in the workspace through the bounded host-process substrate."

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen.workspace = true
serde_json.workspace = true

[lints]
workspace = true

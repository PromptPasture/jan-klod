[package]
name = "tool-fetch"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "fetch tool: retrieve a URL through host-http as readable text, with an SSRF guard on the model-supplied address."

[lib]
# A reactor component: exports tool-world, no `main`.
crate-type = ["cdylib"]

[dependencies]
wit-bindgen.workspace = true
serde_json.workspace = true
guest-fs.workspace = true

[lints]
workspace = true

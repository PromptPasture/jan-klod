[package]
name = "tool-escape-probe"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "A guest that tries to leave the sandbox: raw sockets, the host's stdin, the host's filesystem. Every attempt must fail; the tests assert that it does."

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen.workspace = true
serde_json.workspace = true

[lints]
workspace = true

[package]
name = "jan-klod-host"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[[bin]]
name = "jan-klod"
path = "src/main.rs"

[dependencies]
jan-klod-core = { path = "../core" }

# Examples: spike_gate (Slice 1a gate) and provider_probe (drives a provider's
# llm-provider.complete path end-to-end; needs config + serde_json to read the
# instance section).
[dev-dependencies]
wasmtime.workspace = true
wasmtime-wasi.workspace = true
jan-klod-config = { path = "../config" }
serde_json.workspace = true

[lints]
workspace = true

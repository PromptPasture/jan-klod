# jan-klod configuration.
#
# Extensions are grouped by category (`provider`, `interceptor`, `tool`, ...).
# Each *named* entry under a category is one configured extension instance.
#
# The host reads only two keys from an entry:
#   enabled  — whether to load the instance (omit/false = skip).
#   type     — which wasm component implements it: ext/<category>-<type>.wasm.
#              Defaults to the entry name, so `provider.openai` implies
#              `type: openai` -> provider-openai.wasm. Point several instances at
#              the same `type` to reuse one wasm (several OpenAI-compatible
#              endpoints use provider-openai); give an instance its own `type`
#              when it needs different payloads/headers.
#
# Every other key in an entry is opaque to the core and served back to the
# extension instance through the host-config interface. ${VAR} references are
# expanded from the environment.

extensions:

  # ---- LLM providers: each entry is one configured endpoint ----
  # `type` selects the implementing wasm; reuse provider-openai for any
  # OpenAI-compatible endpoint, or set a dedicated type for a bespoke API.
  provider:
    openai:
      enabled: true
      type: openai  # default, shown for clarity
      base-url: https://api.openai.com/v1
      api-key: ${OPENAI_API_KEY}
      model: gpt-4o-mini
    anthropic:
      enabled: false
      type: anthropic   # distinct wasm: different payload/headers
      api-key: ${ANTHROPIC_API_KEY}
      model: claude-sonnet-4-6
    ollama:
      enabled: false
      # `type: openai`, not `type: ollama`. Ollama serves an OpenAI-compatible
      # API at /v1, and there is no provider-ollama.wasm — this block named one
      # for months, so enabling the most common self-hosted setup resolved to a
      # missing component and left you with no provider at all. No `api-key`
      # either: the provider only sends an Authorization header when one is set.
      type: openai
      base-url: http://localhost:11434/v1
      model: qwen2.5:7b
    lm-studio:
      enabled: false
      type: openai
      base-url: http://localhost:1234/v1
      model: qwen2.5:7b
    groq:
      enabled: false
      type: openai        # yet another OpenAI-compatible endpoint
      base-url: https://api.groq.com/openai/v1
      api-key: ${GROQ_API_KEY}
      model: llama-3.3-70b

  # ---- Interceptors: sandboxed decision hooks the core loop dispatches by phase.
  #      enabled/disabled ONLY — ordering is structural (phase order + load order),
  #      never configured here. See docs/concepts/configuration.md#interceptors.
  #
  #      `persist: true` on an instance lets its `host-storage` writes reach the
  #      session store instead of a private map that dies with the process. It is
  #      off by default and deliberately so: the permission gate keeps standing
  #      grants ("always allow fs:write") in host-storage, and a boundary should
  #      not become permanently open because of a click last week. Grant it to an
  #      interceptor that should remember across restarts, not to the one that
  #      decides what you are allowed to do. Each instance is namespaced to
  #      itself; it cannot read a peer's keys or a session transcript. ----
  interceptor:
    intent-router:
      enabled: true            # before-loop: simple vs agentic classification
    task-router:
      enabled: false           # select-model: applies the `routing:` table below.
                               # Off by default: it costs an extra model call per
                               # turn to classify the task. The routing block does
                               # nothing until this is on.
    context:
      enabled: true            # select-context: trims the replayed conversation to
                               # the model's window, oldest non-system messages
                               # first. On by default because turns now carry the
                               # session's history — without it a long session
                               # grows until the provider rejects the request.
      # context-tokens: 8000   # budget; default suits a small local model
    system:
      enabled: true            # select-model: prepends the standing instructions
                               # the turn runs under. On by default — without it
                               # no system message reaches the model at all.
      # prompt: |              # replaces the built-in text; empty string = none
      #   You are a terse reviewer. Never write files.
    tool-selector:
      enabled: true            # select-tools: expose the active tool set (Slice 2d).
                               # On by default: without it the enabled tool.* below
                               # are never offered to the model.
    permission:
      enabled: true            # tool-call: confirm anything not known read-only.
                               # On by default: the file tools below can write, and
                               # a write must be confirmed before it happens.
                               # Answer `always`/`never` to decide for a whole kind
                               # of action (`fs:write`, `shell:cargo`) instead of
                               # one call. Those decisions last for the run only —
                               # a restart asks again — and never cover an argument
                               # pointing outside the workspace.
      # Three refusals of the same kind of call make it standing: the asking
      # stops and the model is told to stop trying. A denial ends one call and the
      # loop continues, so a model that ignores the hint asks again, and every ask
      # is a dialog in front of a person. Nobody should be worn down into clicking
      # yes — prompt fatigue is how a gate stops meaning anything, and it is an
      # attack as much as an annoyance. The count is per kind of call and per run.
      #
      # The gate is an ALLOWLIST: anything not named here is confirmed. It was a
      # denylist of high-risk verbs (`shell`, `write`, `exec`, …), which cannot
      # name the verbs nobody thought of — `tool-edit` ships the ops `replace`
      # and `insert`, neither of which is `write`, so the tool that modifies
      # files went through ungated from the day it was added. A list of what is
      # safe gates the unknown by construction; the cost is that a new tool
      # prompts until someone classifies it.
      #
      #   safe-calls            — `name` or `name:op` that run unasked. REPLACES
      #                           the default (does not extend it); `[]` confirms
      #                           everything.
      #   allow-absolute-paths  — when true, absolute-path args skip the scope gate
      #   allow-parent-traversal— when true, `..` traversal skips the scope gate
      # Defaults shown below; delete keys to accept them.
      # safe-calls: [find, fs:read, fs:grep, git, edit:view, proc-probe]
      # allow-absolute-paths: false
      # allow-parent-traversal: false
    guardrails:
      enabled: false           # tool-call + after-response + finalize +
                               # select-context: content policy. The permission
                               # gate above asks whether an action may happen;
                               # this one looks at what the text says. Off by
                               # default — a content filter nobody asked for is
                               # a surprise, and with no rules it does nothing
                               # anyway.
      #
      # Rules are DATA: patterns and a decision each, never code. The engine is
      # the `regex` crate, which matches in linear time and cannot backtrack —
      # a pattern that could be made to hang would be a denial-of-service
      # surface inside the thing meant to prevent them. The price is that
      # lookaround and backreferences are refused when the rules load.
      #
      # A malformed rule invalidates the WHOLE set, and the host then fails
      # closed at `tool-call`: a typo stops tool calls until it is fixed,
      # loudly, rather than leaving a guardrail that silently is not there.
      #
      # Full reference: src/extensions/interceptor-guardrails/README.md
      #
      #   deny-tool-arguments   — matched against a call's arguments
      #       pattern    (required)  the expression to match
      #       tool       (optional)  limit to one tool; every tool when absent
      #       reason     (optional)  shown to whoever is refused
      #       decision   (optional)  `block` or `ask`; default `block`
      #   redact                — matched against model output, the final answer,
      #                           and messages on their way to the provider
      #       pattern    (required)
      #       with       (optional)  replacement; default `[redacted]`
      #       reason     (optional)  shown when the rule refuses
      #       decision   (optional)  `replace` or `block`; default `replace`
      #
      # Both lists are optional. A blocking rule wins over redaction wherever
      # both match. Examples, commented out:
      # deny-tool-arguments:
      #   - pattern: "rm +-rf +/"
      #     tool: shell
      #     reason: "a recursive delete of a root path"
      # redact:
      #   - pattern: "sk-[A-Za-z0-9]{16,}"
      #     with: "[redacted api key]"

  # ---- Capability catalogues ----
  registry:
    skills:
      enabled: false
    mcp:
      enabled: false

  # ---- Discrete callable tools ----
  # File tools are jailed to the workspace (the `workspace:` key below, or $PWD
  # when it is absent) and their writes are gated by interceptor.permission.
  #
  # Credential files (.env, *.pem, id_rsa, .netrc, .npmrc, …) are skipped by
  # `find` and by tree-wide `grep`, and an explicit read of one is confirmed and
  # never covered by an "always". Reads and greps are otherwise unprompted — a
  # gate that interrupts every search is a gate people switch off — and everything
  # a tool returns becomes a message sent to the model provider on the next turn,
  # so a silent grep across a repo holding a .env would be the workspace's secrets
  # leaving the machine because somebody searched for a word. A pattern that names
  # such a file explicitly (`**/.env`) still lists it: a name discloses nothing,
  # and hiding it outright would teach the model the file does not exist.
  tool:
    fs:
      enabled: true            # read / write / grep a workspace file
    edit:
      enabled: true            # hash-anchored partial edits (view -> replace/insert)
    find:
      enabled: true            # glob the workspace tree (discovery for read/grep)
    git:
      enabled: false           # read-only repo inspection (status/diff/log/show/
                               # branch) through host-process. Needs `execution:`
                               # below; it cannot modify the repository, so it is
                               # safe to enable wherever execution is allowed.
    shell:
      enabled: false           # run ANY command through host-process. Needs
                               # `execution:` below. Off by default: unlike
                               # tool.git, the command comes from the model.
    fetch:
      enabled: false           # GET a public http(s) URL as text (docs, an error
                               # page). OFF by default on purpose: a fetch is a
                               # read, but the URL is also an exfiltration channel
                               # — path and query carry whatever the model puts
                               # there, so a prompt-injected turn can post your
                               # workspace out one request at a time. Private,
                               # loopback and link-local addresses are refused
                               # even when enabled; `allow-private: true` opts
                               # into your own intranet.
      network: true            # grants this tool outbound HTTP. Tools get no
                               # egress unless their entry says so, so a file tool
                               # cannot start making requests.
      # allow-private: false
    # No web-search component exists. The entry used to sit here disabled, with a
    # note saying so — but a declared instance is a promise the runtime will keep,
    # and `every_declared_instance_resolves_to_a_component` now enforces that every
    # one of them resolves. A block that cannot load is not a placeholder, it is a
    # trap for whoever flips `enabled: true`; the same reasoning retired
    # `store.postgres` and the `agent`/`api`/`chat` categories. To add one, write
    # `tool-web-search` against `tool-callable`, grant it `network: true`, and
    # uncomment:
    #
    # web-search:
    #   enabled: true
    #   network: true
    #   base-url: https://api.search.example/v1
    #   api-key: ${SEARCH_API_KEY}

  # ---- AI agent delegation via ACP ----
  # NOTE: inert. `Runtime::build_agent` never instantiates the `agent` category,
  # and no `agent-*` component exists to instantiate — enabling an entry here
  # resolves to an `ext/agent-claude-code.wasm` that is not built, which `verify`
  # reports as missing. The outbound seam is written (`delegate.rs`, unit-tested)
  # and needs a transport plus wiring; the inbound direction is already possible
  # by POSTing a turn to the REST surface.

  # ---- Network API surfaces ----
  # NOTE: there is no `api.*` extension. The REST surface is built into the core
  # binary and started with `jan-klod-gateway serve --bind <addr>`; an entry here
  # would resolve to an `ext/api-rest.wasm` that does not exist.
  #
  # Authentication is off unless JAN_KLOD_TOKEN is set in the gateway's
  # environment. Set it and every route except /health requires
  # `Authorization: Bearer <token>`; the TUI reads the same variable and sends it.
  # Unset, the surface is open — fine on the default loopback bind, and the
  # gateway warns when you bind elsewhere without one. The key lives in the
  # environment rather than here: a secret in a config file ends up in issues.

  # ---- Chat platform integrations ----
  # NOTE: likewise host-side, started with `jan-klod-gateway telegram`. The bot
  # token is read from the TELEGRAM_BOT_TOKEN environment variable, not from here
  # — a `token:` key in this file would be inert (and a secret in a config file).

# ============================================================================
# Agent behaviour — consumed by the core loop and its interceptors (task-router),
# not the core container itself.
# References below use provider *instance names* from extensions.provider above.
# ============================================================================

# The directory the file and execution substrates are jailed to. Every path a
# tool touches is resolved against this root and anything escaping it is denied.
#
# Omit the key and the working directory is adopted — the repo you launched
# `jan-klod serve` in, which is almost always what you meant. Note that this is a
# grant with a default, NOT default-deny like `execution:` below: file tools work
# out of the box. Two roots are never adopted silently, because a jail that wide
# protects nothing — your home directory (where a shell starts) and a filesystem
# root. In those, file tools are denied and the reason is printed; name a root
# here if you truly mean one of them.
#
# The adopted root is printed at boot, so what the agent can reach is never
# something you have to infer.
# workspace: /path/to/your/repo

# A subprocess gets a REBUILT environment, not the gateway's. It inherits only
# PATH, HOME, CARGO_HOME, RUSTUP_HOME, TMPDIR and the LANG/LC_* locale vars —
# because the gateway's own environment holds OPENAI_API_KEY (this file expands
# it) and JAN_KLOD_TOKEN, and one `env` through tool.shell would put both into
# tool output, the transcript, and the next request to the model provider.
# Name anything else a command genuinely needs, one variable at a time:
#
#   env-passthrough: [GITHUB_TOKEN]
#
# The execution substrate (host-process), used by tool.git and tool.shell.
# DEFAULT-DENY: with this block absent or `enabled: false`, every exec is denied
# and those tools fail closed no matter what `extensions.tool` says. Enabling it
# does not by itself let the model run arbitrary commands — that takes
# `tool.shell` as well; `tool.git` alone can only run the read-only git
# subcommands its contract allows.
# ---- Turn bounds ----
# The cycle cap stops a model that keeps emitting tool calls from spinning — and,
# on a metered endpoint, from spending — without end. Eight is a real constraint
# for coding work: view, edit, run the tests, read the failure, fix, run again is
# already six. A turn that hits the cap now says so in its answer instead of
# returning whatever the last completion happened to contain.
#
# limits:
#   max-iterations: 8

# ---- Outbound network ----
# A component granted `host-http` may reach public destinations, plus every
# endpoint this file already names (a provider's `base-url`, an MCP server's
# `endpoint`). Loopback, private, link-local and unique-local addresses are
# refused otherwise — that is 127.0.0.1 (including this gateway's own port),
# 169.254.169.254 (the cloud metadata service, which hands out IAM credentials),
# and everything on your LAN.
#
# The rule is per-origin rather than a switch because self-hosting means local
# models: allowing Ollama on 127.0.0.1:11434 must not also allow the database on
# 5432. List anything config does not already name, one origin at a time.
#
# network:
#   allow:
#     - http://127.0.0.1:11434

# ---- Persistence (host-side, not an extension) ----
# The conversation transcript and every interceptor's granted `host-storage` live
# in one SQLite file the core owns. It is deliberately not a component: the
# sandbox has no filesystem, so a store guest would need one granted back — and
# the transcript is the most sensitive thing here, so the fewer parties that hold
# it the better. Guests reach it only through `host-storage`, namespaced to
# themselves, and only when granted `persist: true`.
#
# Omit `path` (or the whole block) for an in-memory store: nothing survives a
# restart, which is the right default for trying it out and the wrong one for
# using it.
#
# `cache-dir` is unrelated to the store above: it is where Wasmtime caches
# compiled components (#60), so a second boot skips Cranelift instead of
# recompiling every guest. Omit it for the default, `wasmtime-cache` beside
# this file — shown here only for clarity. The directory is created
# user-private (0700 on unix): a cache hit is deserialized as native code, not
# re-verified, so anyone who could write into it could hand this host bytes it
# would later execute.
storage:
  path: ./jan-klod.db
  # cache-dir: ./wasmtime-cache

# Who may vouch for a component `jan-klod-gateway ext install` puts into `ext/`.
#
# Minisign public keys, exactly as minisign prints them. **Empty means nothing
# is trusted, not "skip the check"**: an install refuses unless a key here
# verifies signatures over *both* the component and its manifest. That is the
# default-deny rule — a grant is a named thing in this file — and it is why the
# list ships empty rather than absent, so the shape is visible before there is
# anything to put in it.
#
# Nothing first-party is signed yet (no tags, no releases), so today every
# install needs the deliberate widening: `--allow-unsigned`, which in turn
# requires `--sha256 <hex>`. There is no combination that lands a component
# with nothing vouching for it.
#
# Note this is the **top-level** `registry`, not `extensions.registry` above —
# that one holds the `skills` and `mcp` catalogue instances. They share a word
# and nothing else.
registry:
  trusted-keys: []
  # Where `ext search`, `ext list --remote` and `ext install <name>` read the
  # published index of components from. An index states what each component
  # asks the host for *before* anything is downloaded, which is the point of
  # generating one.
  #
  # A URL fetches, over the same egress policy an install does — public
  # addresses only, every redirect hop re-checked. Anything that is not
  # http/https is a **path**, so an air-gapped operator can point this at a
  # file and the whole flow works offline.
  #
  # Reading an index grants nothing. `trusted-keys` above still decides what
  # may land in `ext/`, so with the list empty every install from here is
  # refused as untrusted — which is the correct answer until something
  # first-party is signed.
  url: https://promptpasture.github.io/jan-klod/index.json

# Load a component that ships no `<name>.manifest.toml` beside it. Off, because
# a component nobody can inspect before running it is the thing a manifest
# exists to prevent — and `make ext` generates one per guest, so the only way
# to hit this is a component from somewhere else.
#
# Top-level rather than under `extensions:`, where every key must be a category
# of named instances.
# allow-unmanifested: true

execution:
  enabled: false
  timeout-secs: 30            # a command that outruns this is killed
  output-cap: 65536           # captured stdout/stderr bytes per run

  # What a command may do once it is running. Everything above bounds the
  # *caller* — the cwd is jailed to the workspace, the environment is rebuilt,
  # time and output are capped — and none of it bounds the command itself, which
  # runs with your privileges and can read or write anywhere you can.
  #
  # `mode: os` asks the operating system to confine it, and on macOS and Linux
  # it does: Seatbelt via `sandbox-exec`, Landlock via a ruleset. Reads are
  # allowed, writes only under `writable`, and the network only if you say so.
  # On any other platform there is no backend, so the mode resolves to
  # `approval-only` — the confirmation prompt is then the only thing between
  # the model and your filesystem. That is printed at boot with its reason
  # whenever execution is enabled; it is never downgraded quietly.
  #
  # Two paths are granted to every confined command regardless of `writable`,
  # because a command that cannot use them does not run at all rather than
  # running unconfined: `/dev/null`, and a `TMPDIR` the host points inside the
  # workspace. See docs/concepts/security-model.md for both rows.
  sandbox:
    mode: os                  # `os` or `approval-only`
    writable: ["."]           # workspace-relative roots a command may write to
    network: false            # whether a command may reach the network
    require: false            # `true` denies host-process entirely rather than
                              # falling back to approval-only — no command at
                              # all, in preference to an unconfined one. On a
                              # platform with no backend, that means nothing
                              # runs.

# Which provider answers interceptors' model calls (the intent router's
# simple-vs-agentic classification). Names an instance from
# `extensions.provider` above; omit to use the head of the chain below.
# Classification is a two-token question, so pointing this at a small local
# model is the reason the key exists. An unknown name warns and falls back to
# the conservative label, and a classifier failure never fails the turn.
# classifier: ollama

# Provider fallback chain: the order enabled providers are tried in. Each entry
# names an instance from `extensions.provider` above. On a provider error the
# next one is tried; when the chain is exhausted the error surfaces to the user
# (no silent retry spiral).
#
# A name with no enabled instance is skipped with a warning (so the `ollama`
# entry below is inert until you enable it), and an enabled provider this list
# omits still runs — after the ones listed. Omit the whole key to accept boot
# order (alphabetical by instance name).
#
# NOT YET HONOURED: a per-provider `models:` list. Each provider instance serves
# exactly one `model` (set in its own entry above); trying several models within
# one provider needs a runtime model override the provider contract does not have
# yet. Configure a second instance pointing at the same `type` instead.
providers:
  - provider: openai
  - provider: ollama          # local last resort — inert until enabled above

# Task routing: task-type -> model. A classifier picks the closest matching key
# at runtime, and the chosen model is used for that turn.
#
# INERT UNLESS `interceptor.task-router` IS ENABLED (it is off above). Editing
# this block changes nothing on its own — the interceptor is what reads it, and
# enabling it costs one extra model call per turn to classify the task.
#
# Values are **model names, not providers**. The fallback chain is fixed when the
# agent boots, so routing cannot send a turn to a different endpoint; a value like
# `groq/llama-3.3-70b` applies `llama-3.3-70b` to whichever provider answers —
# which is not groq. The `provider/` form is still accepted (older configs use it)
# and warns. To prefer a different endpoint, order `providers:` above instead.
routing:
  code-generation:  gpt-4o
  code-review:      gpt-4o
  file-edit:        gpt-4o
  reasoning:        gpt-4o
  planning:         gpt-4o
  web-search:       gpt-4o-mini
  research:         gpt-4o-mini
  chat:             gpt-4o-mini
  clarification:    gpt-4o-mini
  agent-delegation: gpt-4o
  # user-defined example:
  # data-analysis:  gpt-4o-mini
